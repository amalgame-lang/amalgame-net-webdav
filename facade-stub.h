/* amalgame-net-webdav — stub runtime header.
 *
 * The only C in this package is a binary-safe PUT writer and a
 * binary-safe file copy, which live in inline @c {} blocks inside
 * facade.am using the libc symbols (fopen/fread/fwrite) the amc
 * runtime already links. This header carries no declarations. */
#ifndef AMALGAME_NET_WEBDAV_H
#define AMALGAME_NET_WEBDAV_H
#endif
